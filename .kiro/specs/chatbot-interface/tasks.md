# Implementation Plan

- [x] 1. Create ChatHeader component
  - [x] 1.1 Implement ChatHeader with static positioning outside keyboard-aware layout
    - Create header container with 64-72px height, white background, bottom border
    - Add circular back button (40x40px) with border and left-arrow icon
    - Add centered "Chat" title with semibold 18-20px font
    - Implement onBackPress navigation handler
    - Add accessibility labels for back button
    - _Requirements: 1.1, 1.2, 1.3, 1.5, 7.1_

- [x] 2. Create MessageBubble component
  - [x] 2.1 Implement MessageBubble with sender-specific styling
    - Create bubble container with conditional alignment (flex-start/flex-end)
    - Apply background colors (#F2F2F2 for bot, #7A2EFF for user)
    - Set border radius (18-20px bot, 20-22px user)
    - Apply text colors (#222222 bot, #FFFFFF user)
    - Set max-width constraints (75-80% bot, 60-70% user)
    - Add padding (12px vertical, 14px horizontal)
    - _Requirements: 2.1, 2.2, 2.4, 2.5_
  - [ ]* 2.2 Write property test for bot message styling
    - **Property 1: Bot message styling consistency**
    - **Validates: Requirements 2.1**
  - [ ]* 2.3 Write property test for user message styling
    - **Property 2: User message styling consistency**
    - **Validates: Requirements 2.2**
  - [x] 2.4 Add accessibility labels to MessageBubble
    - Set accessibilityLabel to "Bot says, {text}" for bot messages
    - Set accessibilityLabel to "You said, {text}" for user messages
    - _Requirements: 7.2, 7.3_
  - [ ]* 2.5 Write property test for message accessibility labels
    - **Property 10: Message accessibility labels by sender**
    - **Validates: Requirements 7.2, 7.3**

- [x] 3. Create InputBar component
  - [x] 3.1 Implement InputBar with pill-shaped input field
    - Create input container with 48-52px height, 24-26px border radius
    - Add placeholder text "Ask anything…" with #A0A0A0 color
    - Implement focus state tracking for border style changes
    - Add send button icon (paper plane) positioned inside input on right
    - _Requirements: 4.1_
  - [x] 3.2 Implement border state changes based on focus
    - Apply 1px solid #333333 border when unfocused
    - Apply 1.5px solid #000000 border when focused
    - _Requirements: 4.2, 4.3_
  - [ ]* 3.3 Write property test for input border state
    - **Property 5: Input border state based on focus**
    - **Validates: Requirements 4.2, 4.3**
  - [x] 3.4 Implement send button opacity based on input content
    - Set opacity to 0.5 when input is empty or whitespace-only
    - Set opacity to 1.0 when input contains non-whitespace text
    - _Requirements: 4.4, 4.5_
  - [ ]* 3.5 Write property test for send button opacity
    - **Property 6: Send button opacity based on input content**
    - **Validates: Requirements 4.4, 4.5**
  - [x] 3.6 Implement send action behavior
    - Call onSend and clear input when text is present
    - Do nothing when input is empty or whitespace-only
    - _Requirements: 4.6, 4.7_
  - [ ]* 3.7 Write property test for send action behavior
    - **Property 7: Send action behavior based on input content**
    - **Validates: Requirements 4.6, 4.7**
  - [x] 3.8 Add accessibility labels to InputBar
    - Set input accessibilityLabel to "Message input field, Ask anything"
    - Set send button accessibilityLabel to "Send message, button"
    - _Requirements: 7.4, 7.5_

- [x] 4. Create ChatBody component
  - [x] 4.1 Implement ChatBody with scrollable message list
    - Create ScrollView container with 16px horizontal padding
    - Render messages with 10-12px vertical gap
    - Implement ref forwarding for scroll control
    - _Requirements: 3.1, 3.2_
  - [x] 4.2 Implement smart auto-scroll behavior
    - Track scroll position to detect if user is at bottom (within 50px threshold)
    - Auto-scroll to bottom when new message added if at bottom
    - Preserve scroll position when user is scrolled up
    - _Requirements: 3.3, 3.4_
  - [ ]* 4.3 Write property test for auto-scroll behavior
    - **Property 4: Auto-scroll behavior based on scroll position**
    - **Validates: Requirements 3.3, 3.4**
  - [x] 4.4 Implement chronological message ordering
    - Sort messages by timestamp in ascending order before rendering
    - _Requirements: 3.1_
  - [ ]* 4.5 Write property test for message ordering
    - **Property 3: Message chronological ordering**
    - **Validates: Requirements 3.1**

- [x] 5. Checkpoint - Ensure all component tests pass
  - Ensure all tests pass, ask the user if questions arise.

- [x] 6. Create ChatbotView main component
  - [x] 6.1 Implement ChatbotView with keyboard-aware layout
    - Use KeyboardAvoidingView with behavior="padding"
    - Position ChatHeader outside keyboard-aware container
    - Include ChatBody and InputBar inside keyboard-aware container
    - Handle safe area insets properly
    - _Requirements: 1.4, 5.1, 5.2, 5.3, 5.4_
  - [x] 6.2 Implement message state management
    - Create messages state array
    - Implement addMessage function for optimistic updates
    - Display user message immediately before API response
    - _Requirements: 6.3_
  - [ ]* 6.3 Write property test for optimistic UI update
    - **Property 9: Optimistic UI update on send**
    - **Validates: Requirements 6.3**
  - [x] 6.4 Implement chat history persistence
    - Load chat history from AsyncStorage on mount
    - Save messages to AsyncStorage after each send
    - Use existing chatStorage utility functions
    - _Requirements: 6.1, 6.2_
  - [ ]* 6.5 Write property test for message persistence round-trip
    - **Property 8: Message persistence round-trip**
    - **Validates: Requirements 6.1, 6.2**
  - [x] 6.6 Implement message sending flow
    - Call chatWithNote API with noteId and message
    - Add bot response to messages on success
    - Handle API errors gracefully
    - _Requirements: 4.6_

- [x] 7. Final Checkpoint - Ensure all tests pass
  - Ensure all tests pass, ask the user if questions arise.
