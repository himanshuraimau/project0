/**
 * Flinote Design System — Border radius
 * Default UI radius: radiusLg (16px). radiusFull for pills.
 */

import type { RadiusScale } from './types'

export const radius: RadiusScale = {
  radiusNone: 0,
  radius2xs: 4,
  radiusXs: 6,
  radiusSm: 8,
  radiusMd: 12,
  radiusLg: 16,
  radiusXl: 20,
  radius2xl: 24,
  radius3xl: 32,
  radiusFull: 9999,
}
